import express, { NextFunction, Request, Response } from 'express'
import catchAsync from '../utils/catchAsync'

const generateCourse = catchAsync((req: Request, res: Response, next: NextFunction) => {

})

export default { generateCourse }
